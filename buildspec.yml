version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 6.0
  build:
    commands:
      - dotnet publish -c Release -o ./publish
  post_build:
    commands:
      - zip -r app.zip ./publish
artifacts:
  files:
    - app.zip
